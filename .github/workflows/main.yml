name: RDP

on:
  workflow_dispatch:

jobs:
  secure-rdp:
    runs-on: windows-latest
    timeout-minutes: 3600

    permissions:
      contents: write
      actions: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: Get Public IP
        run: |
          $ip = (Invoke-WebRequest -Uri "https://api.ipify.org").Content
          echo "PUBLIC_IP=$ip" >> $env:GITHUB_ENV
          Write-Host "Public IP: $ip"

      - name: Enable RDP (Nuclear Fix)
        run: |
          # 1. Disable Firewall Entirely (Force-disable all profiles)
          Write-Host "Disabling Windows Firewall..."
          netsh advfirewall set allprofiles state off
          
          # 2. Enable RDP & Disable Network Level Authentication (NLA)
          Write-Host "Configuring RDP Registry..."
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fAllowToGetHelp" -Value 1 -Force
          
          # 3. Ensure Remote Desktop is allowed (Backup)
          Enable-NetFirewallRule -DisplayGroup "Remote Desktop"
          
          # 4. Force Restart RDP Service
          Write-Host "Restarting RDP Service..."
          Restart-Service -Name TermService -Force

      - name: Create RDP User
        run: |
          $password = ConvertTo-SecureString "AnyDesk123!" -AsPlainText -Force
          New-LocalUser -Name "RDP" -Password $password -AccountNeverExpires -ErrorAction SilentlyContinue
          Add-LocalGroupMember -Group "Administrators" -Member "RDP" -ErrorAction SilentlyContinue
          Add-LocalGroupMember -Group "Remote Desktop Users" -Member "RDP" -ErrorAction SilentlyContinue

      - name: Setup Ngrok Tunnel
        run: |
          Write-Host "Downloading Ngrok..."
          Invoke-WebRequest https://bin.equinox.io/c/bNyj1mQVY4c/ngrok-v3-stable-windows-amd64.zip -OutFile ngrok.zip
          Expand-Archive ngrok.zip -DestinationPath . -Force
          
          Write-Host "Configuring Ngrok Auth..."
          .\ngrok.exe config add-authtoken ${{ secrets.NGROK_AUTH_TOKEN }}
          
          Write-Host "Starting Ngrok Tunnel..."
          Start-Process -FilePath ".\ngrok.exe" -ArgumentList "tcp 3389" -NoNewWindow
          
          # Wait for tunnel initialization
          Start-Sleep -Seconds 10
          
          # Retrieve URL from Local API
          $tunnel = Invoke-RestMethod http://127.0.0.1:4040/api/tunnels
          $publicUrl = $tunnel.tunnels[0].public_url
          
          Write-Host "======================"
          Write-Host "NUCLEAR FIX SUCCESS!"
          Write-Host "NGROK RDP ADDRESS: $publicUrl"
          Write-Host "Username: RDP"
          Write-Host "Password: AnyDesk123!"
          Write-Host "======================"

      - name: Keep Alive
        run: |
          while ($true) {
            Write-Host "[$(Get-Date)] RDP Session Active..."
            Start-Sleep -Seconds 300
          }

